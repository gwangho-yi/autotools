[tool.uv.workspace]
members = ["shared", "apps/motion-capture", "apps/color-capture", "apps/motion-clicker", "auto-capture", "auto-clicker"]

[tool.uv.sources]
autotools_shared = { workspace = true }
